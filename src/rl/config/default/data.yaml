data:
  # Dataloaders params
  dataset: RLDataset
  batch_size: 64
  train_split: 0.8
  shuffle: True
  seed: 0
  # Dataclass params
  params:
    replay_size: 1000
    sample_size: 200
    env: "CartPole-v0"
    warm_start_steps: 1000